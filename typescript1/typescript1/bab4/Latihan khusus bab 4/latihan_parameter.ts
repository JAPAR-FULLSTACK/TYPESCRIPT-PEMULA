function perkenalan(nama: string, kelas: string, sekolah: string): string {
  return `Halo, nama saya ${nama}, dari kelas ${kelas}, sekolah ${sekolah}.`;
}

console.log(perkenalan("Muhammad Raffadian pratama", "x RPL ", "SMKS ANTARTIKA 1 SDA"));